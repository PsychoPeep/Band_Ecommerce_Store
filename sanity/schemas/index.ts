import category from './category'
import heroImage from './heroImage'
import product from './product'
import tabs from './tabs'
import tickets from './tickets'

export const schemaTypes = [category, heroImage, product, tabs, tickets]
